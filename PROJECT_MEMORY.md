# PARCELGRID — 한국 시행사용 부동산 타당성 분석 도구

## 본인 정보
- 이름: C
- 경로: /Users/yoonseochoi/Desktop/mywork/koreabuild/PARCELGRID
- 환경: macOS, Next.js 15.5 + TypeScript + pnpm, dev :3000

## 도구의 차별화 (선두주자 못 하는 것)
- Smart Picker: 부지 크기 자동 시나리오 매칭
- 단독/다가구 신축매매 (작은 부지) + 다세대 (큰 부지)
- 최대 시행 가능 인수가 역산 (IRR 10/15/20%, binary search)
- 시장가 vs 시행 가능가 갭 정직 표시

## 완료 라운드 (Git 백업)
- 복구 Phase 1-3
- Round 1A: ScenarioTable / RiskMatrix / CompsTable / PFChart
- Round 2-1: 백엔드 (compute-project comps + MOLIT 4종)
- Round 2-2: 대시보드 PDF p1 풀 복원 (3-column + 사이드바)
- Round D: 최대 시행 가능 인수가 역산
- Round E: 단독/다가구 + Smart Picker
- Round F: calculateCore BuildingType별 (단독 효율 95%)
- Round G: 인수가 추정 정확도 (sizeMultiplier + 가중평균)

## 다음 라운드 — Round H (큰 작업)
**인수가 입력 UX 재설계**:
- 자동 추정 제거 (또는 참고용)
- 사용자가 직접 입력
- 주변 실거래 평당가 분포 그래프/히스토그램
- 부지의 percentile 표시
- 입력 시 "주변 시세 대비 N%ile" 컨텍스트

이유: 강남구 역삼동 같은 큰 부지에서 자동 추정이 2,058억 같은 비현실적 숫자 나옴. 시행사는 인수가 알고 있으니 직접 입력 + 분포 시각화로 컨텍스트 제공이 진짜 가치.

## API 키 (.env.local)
- KAKAO_REST_API_KEY=***REMOVED***
- MOLIT_SERVICE_KEY=***REMOVED***
- VWORLD_API_KEY=***REMOVED***

## 핵심 통찰
- "시장 평균 인수가 ≠ 시행 가능 인수가"
- 자동 추정 X, 분포 시각화 + 직접 입력
- 부지 크기별 시나리오 차등
- 정직한 답

---

# ===== 건축 기획 엔진 설계 문서 (다음 세션 빌드 기반) — 2026-06 =====
**이 문서만 보고 다음 세션에 바로 빌드 시작. C(시행사·법정건축)와 확정한 설계.**

## 협업 원칙 (C의 명령 — 필독)
- **애매하면 추측 말고 먼저 질문한다.** 특히 법정 건축 기준.
- Claude = 코딩/UX 구조 담당. C = 법정 건축 담당. 각자 강점 존중.
- 목업·하드코딩 절대 금지. 계산되는 사실 또는 사용자 입력만.

## 핵심 철학 — 하이브리드 도구
1. **우리가 계산 = 법적 최대치 (Envelope)** — 사실
2. **유저(시행사)가 조정 = 실제 세대계획** — 사람 판단
3. **시스템이 실시간 재계산** — 주차·엘베·일조·사업성
업계 순서: 토지 → 법규 → Envelope → Mass → Unit Plan. (하우빌드·플렉시티 방식)
세대계획은 Envelope의 하위 개념. 그래서 **최대치가 항상 먼저**.

## ⭐ 면적 필드 분리 (건축 데이터 엔진 핵심 — 처음부터 분리)
C 강조: "이 7~8개 필드는 처음부터 분리해야 나중에 안 갈아엎는다."
- **대지면적 (lotArea)**: 건폐율·이격·건축선 기준
- **건축면적 (buildingArea)**: 대지 × 건폐율 (1층 바닥 최대)
- **연면적 GFA (grossFloorArea)**: 실제 사용면적 (주차·제외분 포함) — "우리집 40평"
- **용적률 연면적 FAR (farFloorArea)**: 법규용 (지하·주차·다락 제외) — "용적률 27평"
- 건폐율(bcr) / 용적률(far) / 층수 / 주차대수
V1: GFA + FAR 2개만. V2: 지하·다락·발코니 추가. V3: 자동계산.
※ "왜 34평인데 용적률 28평이냐" 질문에 답하려면 2개 분리 필수. 하나로 가면 나중에 구조 다 갈아엎음.

## ⭐ 최대치 공식 (C가 건축실무로 확정 — envelope.ts에 구현됨)
- **최대 건축면적 = 대지 × 건폐율** ✅
- **최대 용적률 연면적 = 대지 × 용적률** ✅
- **이론 층수 = floor(용적률 ÷ 건폐율)** — 버림. "이론"이라 명시 (실제 아님)
- **추천 층수 = 비율 구간** (1.5↓ 2층 / 1.5~2.5 2~3층 / 2.5~3.5 3층 / 3.5↑ 4층 이상)
- ⚠️ "최대 층수" 표현 금지 — 실무상 존재 안 함. 높이·일조·도로사선이 실제 층수 결정.
- STEP3 법규필터(높이제한, 북측 일조사선, 도로폭)는 데이터 확보 후 (미구현).

검증된 예 (38평=125.6㎡):
- 건폐50 용적100 → 이론2 추천 2~3층
- 건폐60 용적250 (쌍문동) → 건축면적 22.8평, 용적연면적 95평, 이론4, 추천 4층 이상

## ⭐ 유형별 입력 분기 (C 확정)
- **단독주택**: 세대수 = 1 고정. 입력 불필요. 층수만 (추천 범위 내).
- **빌라(다세대/다가구)**: 세대수 + 세대당 전용평수 입력 필수.
  - 세대당 전용면적이 주차를 결정 (§27): 60㎡↓ 0.7대, 60㎡↑ 1대, 30㎡↓ 0.5대, 도시형 0.6대.
  - 제약: 세대수 × 세대당 면적 ≤ 최대 용적률 연면적.
  - 건축사 방식 = 세대계획 먼저, 주차 산정 나중.

## ⭐ 새 페이지 구조 (Claude 권장 — 확정)
- **새 라우트 `/projects/[id]/envelope`** (기존 상세=분석결과와 분리. 건축기획 흐름 독립).
- **한 페이지 세로 스크롤, STEP이 쌓임** (위자드 X). 이유: "조정과 결과를 동시에 본다" = 실시간 재계산의 핵심. 위(최대치 고정) ↔ 아래(세대조정) 연결 유지.
- **기존 Double Edge 디자인 시스템 따름** (ui-kv, ui-sb-section, 카드 등). 새로 디자인 X.

## STEP 구조 (C 확정)
- **STEP 1 토지분석**: 대지면적·건폐율·용적률·최대건축면적·최대연면적·이론층수·추천층수 ← envelope.ts (오늘 만듦)
- **STEP 2 추천 시나리오**: 2층 단독 / 3층 다가구 / 4세대 빌라 등
- **STEP 3 세대수 조정 슬라이더**: 4세대 ↔ 6 ↔ 8 → 실시간 재계산
- **STEP 4 평면 생성**: 먼 미래

## 다음 세션 빌드 순서
1. envelope.ts 확인 (이미 생성: apply_envelope_engine.py). tsc 통과 확인.
2. `/projects/[id]/envelope` 라우트 + 페이지 생성. 기존 디자인 시스템 사용.
3. STEP 1 표시: envelope.ts 결과를 카드로 (대지·건폐·용적·건축면적·연면적·이론층수·추천층수). 평 단위 병기.
4. (그 다음) STEP 2 추천 시나리오 → STEP 3 세대수 슬라이더 + 실시간 재계산.

## 파일
- src/lib/finance/envelope.ts (calcEnvelope, sqmToPyeong) — 오늘 생성.
- parcel.lotArea, maxBCR, maxFAR 사용 (compliance.ts에서 접근 패턴 참고).
- ⚠️ BuildingProgram에 gfa 없음 — far(%)만. 연면적은 lotArea × far/100로 계산.

---

# ===== 층고·층수·조정 슬라이더 설계 (C 확정 추가) — 2026-06 =====
**이전 설계 문서에 이어짐. 층수 산정 방식 정밀화.**

## 핵심 변경 — 층수는 "층고로 결정된다"
사용자가 층수를 직접 고르는 게 아니라:
1. **추천 층고가 기본값**으로 나옴 (예: 1층 3.3m, 2층 이상 2.8m)
2. **사용자가 층고를 조정** (슬라이더)
3. **층고 + 높이제한으로 층수 자동 재계산**
예: 높이제한 9m, 층고 3.0m → 3층. 층고 2.8m로 줄이면 → 3층(8.4m, 여유). 층고가 층수를 만든다.

## 조정 가능한 슬라이더 (확정)
- **용적률 슬라이더**: 180% ↔ 250% (상한 내에서 조정)
- **층고 슬라이더**: 추천 층고 ↔ 조정 → 층수 재계산
- **세대수 슬라이더** (빌라): 최대 N세대 ↔ 조정 → 주차 재계산
- 모두 **최대치가 상한(기준점)**, 사용자가 그 아래로 조정.

## 최대치를 기준점으로 (C+협업자 확정)
- 최대치를 먼저 보여주고 사용자가 조정. **최소치 X**.
- 이유: 건축주는 최대 수익률 목표로 시작. 최소치 보여주면 "겨우 이거?" → 신뢰 하락.
- 최대 = 이상적 출발점, 거기서 현실로 내려가며 조정.

## ⭐ 높이제한 산정 (C 확정 — 하이브리드, 가장 사실에 가까움)
**1차 자동** (시행사는 최고높이를 모름 — 우리가 계산):
- 용도지역 + 건폐율 + 용적률 + 도로사선 → 예상 층수
**2차 재계산** (특수 규제 있을 때):
- 고도지구 / 최고높이 제한 / 경관지구 / 공항 고도제한 → 재계산
- 절대높이 있으면 그 값 사용, 없으면 용적률·사선·층고로 추정.
※ 사용자 입력에만 맡기면 UX 깨짐 (시행사는 "몇 층 가능?"을 묻지 "최고높이 몇 m?"를 안 물음).

### ⚠️ 다음 세션 선결 — 특수규제 데이터 출처 확인
고도지구·경관지구·공항제한을 **실제로 가져올 수 있는지** 먼저 확인 (안 그러면 또 가짜).
- V월드에 고도지구/경관지구 레이어 있는지 (LT_C_* 계열?) curl 테스트.
- 없으면: 1차 자동(용적률·사선)만 하고, 특수규제는 "해당시 사용자 확인" 표시.
- 절대 추측으로 높이제한 박지 말 것.

## 업데이트된 빌드 순서 (다음 세션)
1. envelope.ts에 층고→층수 로직 추가 (높이제한 ÷ 층고). 단 높이제한 데이터 확인 후.
2. `/projects/[id]/envelope` 페이지: STEP1 최대치 카드 + 슬라이더(용적률·층고·세대수).
3. 슬라이더 조정 → 실시간 재계산 (연면적·층수·주차·수익).
4. 특수규제(고도지구 등)는 데이터 출처 확인 후 2차 재계산 레이어로.

---

# ===== STEP 3 층수 조정 + 층수↔세대수 연동 (추가 완성) — 2026-06 =====

## 추가된 것 (envelope 페이지)
### STEP 3 층수 조정 (sun-envelope.ts 확장)
- checkFloorsByLaw: 층수별 가능 여부 — 정북일조 + 용적률 동시 검토
- ⭐ 0.4 같은 임의 비율 제거 — 전부 boundary 기하:
  - northSouthDepthM: 남북 깊이 (위도차)
  - eastWestWidthM: 동서 폭
  - 최소 건물 깊이 = 건축면적 ÷ 폭
  - 북측 가능 이격 = 남북깊이 - 건물깊이
- 판정: ok / marginal / infeasible + 이유 (정북일조? 용적률?)
- SUN_CHECK_CAVEAT: 개략 검토 문구 필수 표시 (건물배치·평면깊이·조례로 달라짐)
- 쌍문동: 남북 13.4m, 동서 15.1m, 건물깊이 5.0m, 가능이격 8.3m
  → 4층까지 가능 (용적률이 먼저 막음), 5층 용적률 초과
- 층고 3.0m 균일 (MVP). 층수 직접조정 → 필요높이 역산. V2 주택 1F3.3/2F2.8, V3 유형별

### 층수↔세대수 연동 (C 로직 — 중요)
- ⭐ 층수 → 세대수 상한 결정 (O): 층수↑ → 가능연면적↑ → 세대수 max↑
- ⭐ 세대수 → 층수 자동상승 (X): 강제변경 안 함. 가능범위 제한/경고만
- "세대수 줄이면 층수 올라간다"는 틀림 — 연면적 여유만 생김. 층수상한은 용적률·정북일조·고도가 결정
- 로직: 층수 선택 → 가능연면적 → 최대세대수 → 슬라이더 max 제한 → 초과시 "최소 N층 필요"
- gfaAtFloors = min(maxFar, maxBuild × 층수), maxUnitsAtFloors = floor(gfa/세대당면적)
- minFloorsForUnits: "이 세대수 넣으려면 최소 몇 층" (5층으로도 불가면 표시)
- 쌍문동: 2층 2세대 / 3층 4세대 / 4층 5세대 / 5층 5세대(용적률막힘)

## envelope 페이지 현재 상태 (완성도 높음)
- useState 4 (farPct, selectedType, units, floors)
- useMemo 5 (env, sun, scenarios, selected, floorChecks) — 전부 early return 위
- STEP 1 최대치 + 용적률슬라이더 / 정북일조 V1 카드 / STEP 2 시나리오 / STEP 3 세대수+층수+연동
- 전부 진짜 데이터, 가정 제거 (0.4 비율도 기하계산으로)

## 다음 세션 후보 (우선순위 미정)
1. 3D 매싱에 envelope 결과 반영 (정북일조 계단식 깎임, 층수·세대수)
2. store 통합 (envelope = 단일 진실 소스 → 사이드바·기존페이지 동기화)
3. 재무 연결 (envelope 시나리오 → 기존 calculateScenario 사업성)
4. 주변 건물 회색 박스 (lt_c_bldginfo)
5. 정북일조 V2~4 (도로판별·다중경계·조례)
6. 세대당 면적도 사용자 입력 (현재 50㎡ 추정 고정)

---

# ===== STEP 3 주차 연동 (정밀화 완료) — 2026-06 =====

## 추가된 것 (envelope 페이지)
### 세대당 면적 + 주차 연동 (C 설계 구조)
- 구조: Envelope → 연면적 → 세대수 → 평균 세대면적 → parking.ts → 주차대수 → 가능여부
- ⭐ parking.ts = 유일한 법규 엔진. UI는 계산만, 법규는 parking.ts.
  (법 바뀌어도 한 곳만 수정 — UI에 "60㎡ 1대" 박지 않음)
- 세대당 면적: 연면적÷세대수 자동 (시행사 사고 흐름: 총평수→세대수→세대당평수)
- 주차: calcParking(type, gfaAtFloors, units, 0) 호출
- 표시: "세대당 평균 N평" + "필요 주차 K대" + 근거(parking.basis)

### parking.ts 확인됨 (재사용)
- calcParking(type: BuildingType, gfaSqm, residentialUnits, retailUnits) → ParkingResult
- ParkingResult: requiredCars, basis, exemptPossible, estimated, reference
- 내부: exclusivePerUnit = (gfa/units) × EXCLUSIVE_RATIO(0.75)
- 다가구 §27: 30㎡↓ 0.5대, 60㎡↓ 0.7대, 60㎡↑ 1.0대 / 도시형 0.5~0.6
- 근생: RETAIL_AREA_PER_CAR=134㎡/대
- 쌍문동 다가구 3층 4세대: 세대당 17.2평(전용 43㎡) → 0.7대 → 주차 3대

## 세대당 면적 커스터마이징 — 의도적 제외 (C 판단)
- 핵심은 최대치→층수→세대수→주차 연결. 세대별 면적 커스텀은 평면(STEP4) 단계 일.
- MVP엔 과함. 자동계산(연면적÷세대수)으로 충분.

## STEP 3 정밀화 완료 — envelope 흐름 탄탄
STEP 1 최대치 → 정북일조 V1 → STEP 2 시나리오 → STEP 3(층수·세대수·연동·주차)
전부 진짜 법규 계산 (envelope/sun/scenario/parking 엔진), 추측·가정 제거.

## envelope 페이지 최종 상태
- useState 4 (farPct, selectedType, units, floors)
- useMemo 6 (env, sun, scenarios, selected, floorChecks, parking) — 전부 early return 위
- import: envelope, sun-envelope, scenario-envelope, parking, types

## 다음 세션 후보 (3D는 C가 따로 계획 있음)
1. store 통합 (envelope = 단일 진실 소스 → 사이드바·기존페이지 동기화)
2. 재무 연결 (envelope 시나리오 → 기존 calculateScenario 사업성)
3. 3D 매싱 반영 (정북일조 계단식) — C 별도 계획 있음
4. 주변 건물 회색 박스 (lt_c_bldginfo)
5. 정북일조 V2~4 (도로판별·다중경계·조례)
